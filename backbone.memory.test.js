suite('Backbone Memory Sync Adapter', function() {
    test('backbone should be present', function() {
      assert.ok(Backbone);
    });

    test('jquery should be present', function() {
      assert.ok($);
    });

});
